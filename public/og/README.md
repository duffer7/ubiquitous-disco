# Open Graph assets

`orbit-cover.png` (1200×630) is referenced by the site metadata in
`src/shared/config/metadata.ts` and by the Open Graph / Twitter card tags.

Drop the production cover image here as `orbit-cover.png`.

Design brief for the cover:

- Dark futuristic Orbit Command Center UI on `#09090B`
- Glowing member nodes connected around a central hub
- Indigo → violet → pink gradient lighting (`#6366F1 → #8B5CF6 → #EC4899`)
- The Orbit logo (see `public/logo.svg`) plus the "Control your product
  universe." tagline
